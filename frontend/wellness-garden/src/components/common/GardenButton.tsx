import { Button, styled } from "@mui/material";

export const GardenButton = styled(Button)({
    width: "340px",
    height: "72px",
    fontSize: "1.6rem",
    fontWeight: "600",
    marginBottom: '30px',
  }) as typeof Button;